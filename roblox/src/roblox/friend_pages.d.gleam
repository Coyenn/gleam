// Generated class bindings for Roblox API
import roblox/types.{type FriendPages, type Instance, type Object, type Pages}

/// Treats `FriendPages` as its Roblox ancestor `Pages`.
///
/// This is an upcast only; it does not check or change the underlying Roblox object.
@luau.global("(function(x) return x end)")
pub fn as_pages(instance: FriendPages) -> Pages

/// Treats `FriendPages` as its Roblox ancestor `Instance`.
///
/// This is an upcast only; it does not check or change the underlying Roblox object.
@luau.global("(function(x) return x end)")
pub fn as_instance(instance: FriendPages) -> Instance

/// Treats `FriendPages` as its Roblox ancestor `Object`.
///
/// This is an upcast only; it does not check or change the underlying Roblox object.
@luau.global("(function(x) return x end)")
pub fn as_object(instance: FriendPages) -> Object
